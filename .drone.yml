# ---
# kind: pipeline
# type: docker
# name: default

# workspace:
# path: /drone/src

# steps:
#   - name: lint
#     image: python:3.7-alpine
#     commands:
#       - python -m pip install -r requirements.txt
#       - flake8

---
kind: pipeline
type: docker
name: linux-amd64
platform:
  arch: amd64
  os: linux
steps:
- name: test
  image: golang:1.14.4
